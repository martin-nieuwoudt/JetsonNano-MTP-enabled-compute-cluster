# STATUS — 2026-07-13 (end of day)

## Cluster worker flash — where we are

### DONE
- **Pristine backup** of node1's 64 GB card: `Jetson_Worker_Node1_FullBackup_2026-07-12.img` (59.48 GiB) — UNTOUCHED safety copy.
- **Shrunk worker baseline**: `Jetson_Worker_Baseline.img` (27 GiB) — filesystem 25G, partition 26G, container 27G, backup GPT moved to end.
- **Spin test PASSED** on node1 slot as `nano01` (.151): boots, `rpc-server` LISTENING on 50052, reaches node0 (TCP 50052 OPEN), auto-expand logic works manually (grew 25G→30G).
- **Auto-expand bug FIXED**: `phase3b_firstboot.service` used `ConditionFirstBoot=yes` which NEVER fires on cloned images (systemd regenerates /etc/machine-id before the unit is evaluated). Removed it; service now runs every boot (script is idempotent). Verified `Active: active (exited)` on reboot. Fix applied to BOTH `code/phase3b_firstboot.service` AND baked into `Jetson_Worker_Baseline.img`.
- **#5 verified**: node0 (`nano`, .150) up, `rpc-server` LISTENING on 0.0.0.0:50052, node0→worker .151:50052 = OPEN. Cluster "join" = rpc-server TCP-reachable on 50052. Works both directions.
- **Per-worker re-identification SCRIPTED** (todo #6 done):
  - `code/prepare_worker_image.ps1 -WorkerNumber 1..10` — copies baseline → `Jetson_Worker_nanoNN.img`, offline-patches (WSL loop mount) `/etc/hostname`, `/etc/hosts` (fixes stale `127.0.1.1 nano`), and NM keyfile `address1` (static IP from `cluster_config.py`: .151=nano01 … .160=nano10).
  - `code/flash_worker.ps1 -WorkerNumber 1..10` — prepare → launch Rufus `-i <img> -g -x` (pre-selects image; HUMAN clicks Start, Rufus has no unattended write) → wait for boot → verify hostname/IP/rpc LISTENING/50052 + node0→worker 50052.
  - Both scripts pass PowerShell syntax check. prepare verified live for worker #2 (nano02/.152). flash verify logic dry-run against live worker #1 = PASS.

### READY FOR TOMORROW
- Flash workers 4–10 from `Jetson_Worker_Baseline.img` using `flash_worker.ps1`.
- Worker #2 image already prepared: `Jetson_Worker_nano02.img` (in Cluster root).
- For each: `cd C:\Users\marti\Desktop\Cluster\code; .\flash_worker.ps1 -WorkerNumber N`

### DEPLOYED THIS SESSION (evening)
- **Worker #3 (`nano03`, .153) JOINED the cluster.** Card `Jetson_Worker_nano03.img`
  was already flashed (prepared earlier); user plugged it in and powered on.
  Verified live: `HOST=nano03`, `IP=192.168.50.153/24`, `rpc-server LISTENING`
  on 50052, node0→.153:50052 = OPEN. Dashboard now shows **4 / 11** online
  (.150 WARN, .151/.152/.153 PASS).
- Helper `code/verify_worker3.ps1` written: waits for .153 to come up then verifies
  identity + RPC + node0 reachability (no Rufus step, since card was pre-flashed).
  NOTE: its static-IP grep is a false-negative trap — the probe assigns the IP to a
  var but the original version didn't echo it; the node's IP is in fact correct
  (`ip -4 addr show eth0` → `192.168.50.153/24`). Trust HOST/RPC/join checks.

### DEPLOYED THIS SESSION (later)
- **Worker #4 (`nano04`, .154) JOINED the cluster.** User flashed
  `Jetson_Worker_nano04.img` via balenaEtcher (not Rufus), plugged in and powered on.
  Verified live: `HOST=nano04`, `IP=192.168.50.154/24`, `rpc-server LISTENING` on
  50052, node0→.154:50052 = OPEN. Dashboard now shows **5 / 11** online
  (.150 WARN, .151/.152/.153/.154 PASS, .155–.160 FAIL).
- **`flash_worker.ps1` probe bug FIXED**: the verify probe only echoed `RPC=...`
  and never printed `HOST=`/`IP=`, so `$hostOk`/`$ipOk` were ALWAYS false → every
  run reported PARTIAL/FAIL even when the node was correct (same false-negative
  class as `verify_worker3.ps1`). Fixed the probe to echo `HOST=$(hostname)` and
  `IP=$(ip -4 addr show eth0 ...)`. Re-run with `-SkipPrepare` now reports a true
  **PASS** for nano04. Trust the corrected HOST/IP/RPC/join checks.

### MODEL TRANSFER — ALL DONE (this session)
All 3 new models pushed to node0 SSD `/mnt/ssd/models` via `model_sync.py push`
(SCP over SSH; SMB to Windows is blocked by design, not used for transfer):
- ✅ `Phi-3-mini-4k-instruct-Q6_K.gguf` (3.14 GB) — PUSH OK
- ✅ `Qwythos-9B-Claude-Mythos-5-1M-MTP-Q8_0.gguf` (9.79 GB) — PUSH OK
- ✅ `DeepSeek-R1-Distill-Qwen-32B-Q6_K_L.gguf` (27.3 GB) — PUSH OK (14:55)
SSD now holds 7 GGUFs total (incl. Codestral-22B, Llama-3.3-70B, Qwen2.5-72B
from Jul 10-11). SSD usage 125G/458G (29%), 310 GB free. All 3 new models'
SHA256 were verified against HF official pages earlier this session.

### NODE5 — JOINED (this session, evening)
- `Jetson_Worker_nano05.img` prepared earlier (hostname `nano05`, static IP
  `192.168.50.155/24`, PC pubkey injected). User flashed + powered on.
  Verified live via `flash_worker.ps1 -WorkerNumber 5 -SkipPrepare`:
  `HOST=nano05`, `IP=192.168.50.155/24`, `rpc-server LISTENING` on 50052,
  node0→.155:50052 = OPEN. **RESULT: PASS.**
  Dashboard now shows **6 / 11** online (.150 WARN, .151–.155 PASS, .156–.160 FAIL).

### SSD WINDOWS VISIBILITY — FIXED & PERSISTENT
- Root cause of "SSD not showing in Windows": node0 UFW had no rule for SMB
  (139/445). Added `sudo ufw allow 139/tcp` + `445/tcp`. Now `\\192.168.50.150\ssd`
  is browsable from Windows.
- Made persistent on Windows: Z: mapped with `/persistent:yes` (registry-backed
  `HKCU\Network\Z`), plus scheduled task `ClusterSSD-Z-Reconnect` (runs on logon
  + every 5 min, skips if already mapped, only connects when node0 pings) so the
  SSD drive reappears automatically whenever the cluster is on. Task registered
  elevated (RunAs) — State: Ready.

### KNOWN / EXPECTED (not bugs)
- SSH node→node (worker→node0) = `Permission denied (publickey)` — EXPECTED. Handled by `code/push_keys.sh` (per-node key distribution), deliberately NOT baked into image. RPC compute path (TCP 50052) is what matters and works.
- Rufus = Store app (GUI only) + portable `C:\Installers\Media\Rufus Installer.exe` (CLI pre-select only, no auto-write). Etcher GUI also fine.

### OPEN QUESTION (user decision)
- Wire `push_keys.sh` into flash flow, or leave key distribution as separate post-deploy step? (Asked, not yet answered.)

### KEY FILES
- `C:\Users\marti\Desktop\Cluster\Jetson_Worker_Baseline.img` — THE image to flash (27 GiB, fixed auto-expand).
- `C:\Users\marti\Desktop\Cluster\Jetson_Worker_Node1_FullBackup_2026-07-12.img` — pristine 64 GB safety copy, DO NOT TOUCH.
- `C:\Users\marti\Desktop\Cluster\Jetson_Worker_nano02.img` — prepared worker #2.
- `C:\Users\marti\Desktop\Cluster\code\prepare_worker_image.ps1`, `code\flash_worker.ps1`.
- Repo memory: `.memories/repo/cluster_autoshrink.md` (full re-identification + autoshrink notes).

---

## RESEARCH WORKLOAD FRAMEWORK — BUILT THIS SESSION (evening)

**What it is:** the cluster's first real workload — a 3-phase research loop driven by an
experimental theory (not for public release). The orchestration code is retained; the
theory description docs were removed from the repo.

### Phase architecture
- **Phase 1 (large model):** strategy → DAG of tasks (`code/allocator/task_graph.py`).
- **Phase 2 (small models, on-node):** execute via 6 method harnesses in `code/methods/`
  (marl, montecarlo, thermo_ca, kl_div, lean, bayesian). Compute stays on-node; only JSON returns.
- **Phase 3 (large model) — load-bearing:** judge evaluates the results, critiques the
  math, proposes edits.

### Delivered (functional code, retained)
- **`code/allocator/judge_rubric.py`** — SINGLE SOURCE OF TRUTH: `PROPOSITIONS` (P1–P6,
  each mapped to methods + pass conditions), `CRITIQUE_RULES` (6 known math defects),
  `VERDICT_THRESHOLDS` (pass_frac 0.66, cap-at-partial if HIGH open, stances).
- **`code/allocator/judge.py`** — Phase-3 engine: `evaluate_propositions`, `critique_methods`,
  `propose_edits` (HIGH→LOW), `_aggregate` → stance, `judge_to_json`, `judge_with_model` (async hook).
- **`code/allocator/orchestrator.py`** — `stage3_judge()` replaced the dry-run stub with the
  real judge; `import json` + `await asyncio.sleep(0)` added to satisfy SonarQube (no errors).
- **MCP `cluster.method.*` tools** (`code/mcp/cluster_mcp_server.py`): `method_list`,
  `method_run(method, node_ip="", overrides="", timeout_s=120)` (SSH on-node if node_ip set),
  `method_push` (SCP `../methods` → every node's `REMOTE_METHODS_DIR=/home/jetson/methods`).
  `mcp 1.28.1` installed in `.venv`.

### Verified
- `get_errors` clean on orchestrator.py, judge.py, judge_rubric.py.
- End-to-end smoke test (all 6 harnesses → judge) **RUNS**.

### OPEN (next)
- Flash workers .155–.160 so Phase 2 runs distributed.
- Close the known circularities (replace hardcoded constants with parameter sweeps) →
  judge should re-grade toward SUPPORTED.
- Verified end-to-end: **5 coverage gaps** found — C1 Viability Kernel, C5 England
  bound, C8 TIEP/stability constraint, C9 Jevons paradox, C18 Recursive Viability.
  13/18 claims covered, 0 evidence gaps, 0 orphan compute. Stance stays PARTIAL.
- `get_errors` clean on judge.py + judge_rubric.py. Doc + STATUS updated.
